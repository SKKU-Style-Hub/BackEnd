const express = require('express')
const router = express.Router()
const MarketClothing = require('../models/clothing')

router.post('/api/market/seller/create', (req, res) => {

})

module.exports = router